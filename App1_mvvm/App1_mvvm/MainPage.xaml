<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:local="clr-namespace:App1_mvvm.ViewModel"
             x:Class="App1_mvvm.MainPage">

    <StackLayout>

        <Label Text="Add produtos"></Label>
        
        <Entry Text="{Binding Nome}" Placeholder="Nome Produto"></Entry>
        <Entry Text="{Binding Quantidade}" Placeholder="Quantidade"></Entry>
        
        <Button Text="Add" Command="{Binding SaveCommand}"></Button>
        <Button Text="Erase" Command="{Binding EraseCommand}"></Button>

        <Label Text="{Binding Mensagem}"></Label>
    </StackLayout>

</ContentPage>
